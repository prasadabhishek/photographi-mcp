## 4. Thresholds
- For "culling", a threshold of `0.4` is neutral. Suggest `0.5` or `0.6` if the user wants a stricter "clean" result.
- Always recommend `mode="move"` as the fastest visual way for the user to see the result.
